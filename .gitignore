# 서버별 실제 도메인 값이 들어가는 nginx 설정. deploy/nginx/conf.d/default-ssl.conf.template을
# 참고해서 서버에서 직접 생성한다 (README 배포 가이드 참고). git으로 추적하면 서버에서 수동으로
# 적용한 내용이 배포 스크립트의 git pull과 충돌하므로 추적하지 않는다.
deploy/nginx/conf.d/default.conf
